# Init Job to Trigger Secret Creation from Key Vault
# This job mounts the SecretProviderClass volume to trigger secret creation
# Run this in your pipeline before deploying the application
apiVersion: batch/v1
kind: Job
metadata:
  name: playlistmanager-init-secret
  namespace: muzika
  labels:
    app: playlistmanager
spec:
  ttlSecondsAfterFinished: 60  # Auto-cleanup after 60 seconds
  template:
    metadata:
      labels:
        app: playlistmanager
        job-name: playlistmanager-init-secret
    spec:
      serviceAccountName: playlistmanager-sa
      restartPolicy: OnFailure
      containers:
      - name: init-secret
        image: busybox:latest
        command: ["sh", "-c", "echo 'Secret creation triggered' && sleep 5"]
        volumeMounts:
        - name: secrets-store
          mountPath: "/mnt/secrets-store"
          readOnly: true
      volumes:
      - name: secrets-store
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: playlistmanager-azure-keyvault
